<a href="recipe.php?id={{ row.id }}">
    <div class="row list-group-item" style="
            background: url('{{ row.image_lien }}') no-repeat left center;
            background-size: 25%;" data-ajax="true" data-target="recipeEdit.php" data-toggle="{{ row.id }}">
        <div href="#" class="food-list col-xs-9">
            <div class="row">
                <div class="col-md-12">
                    <span class="recipy-title">{{ row.title }}</span>
                </div>
                <div class="col-md-12">
                    <div class="row">
                        <div class="col-md-12 recipy-content">
                            <span class="">{{ row.contenu | truncate(100) }}</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</a>
